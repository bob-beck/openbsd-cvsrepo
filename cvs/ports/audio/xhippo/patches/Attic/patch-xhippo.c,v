head	1.2;
access;
symbols;
locks; strict;
comment	@ * @;


1.2
date	2001.08.11.16.08.55;	author kevlo;	state dead;
branches;
next	1.1;

1.1
date	2001.06.06.10.01.48;	author kevlo;	state Exp;
branches;
next	;


desc
@@


1.2
log
@upgrade to version 3.2
@
text
@--- xhippo.c.orig	Wed Jun  6 09:03:19 2001
+++ xhippo.c	Wed Jun  6 09:05:04 2001
@@@@ -611,13 +611,14 @@@@
 
   /* Deal with non-options */
   while (optind < argc) {
-    if (mode_commandline_songs) {
+    if (mode_commandline_songs)
       add_file(argv[optind++]);
-    } else if (mode_commandline_dirs) {
-      add_directory(argv[optind++]);
-    } else {
+    else
       read_playlist(argv[optind++]);
-    }
+#ifdef HAVE_NFTW 
+   if (mode_commandline_dirs)
+      add_directory(argv[optind++]);
+#endif
   }
   
   /* Show the list and scrollbar. */
@


1.1
log
@- upgrade to version 3.1
- bump NEED_VERSION
@
text
@@

