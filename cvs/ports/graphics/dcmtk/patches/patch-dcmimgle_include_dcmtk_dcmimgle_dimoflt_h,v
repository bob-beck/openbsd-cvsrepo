head	1.1;
access;
symbols;
locks; strict;
comment	@# @;


1.1
date	2017.05.06.16.59.31;	author espie;	state Exp;
branches;
next	;
commitid	SHwRroKG0LTXjEJc;


desc
@@


1.1
log
@let it build with clang... a shitload of the same
@
text
@$OpenBSD$
two phase name lookup

Index: dcmimgle/include/dcmtk/dcmimgle/dimoflt.h
--- dcmimgle/include/dcmtk/dcmimgle/dimoflt.h.orig
+++ dcmimgle/include/dcmtk/dcmimgle/dimoflt.h
@@@@ -114,11 +114,11 @@@@ class DiMonoFlipTemplate
             if (this->Data != NULL)
             {
                 if (horz && vert)
-                    flipHorzVert(&pixel, &this->Data);
+                    this->flipHorzVert(&pixel, &this->Data);
                 else if (horz)
-                    flipHorz(&pixel, &this->Data);
+                    this->flipHorz(&pixel, &this->Data);
                 else if (vert)
-                    flipVert(&pixel, &this->Data);
+                    this->flipVert(&pixel, &this->Data);
             }
         }
     }
@
