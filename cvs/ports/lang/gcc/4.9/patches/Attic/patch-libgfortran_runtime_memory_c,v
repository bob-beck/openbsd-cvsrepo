head	1.2;
access;
symbols;
locks; strict;
comment	@# @;


1.2
date	2014.11.20.19.54.41;	author pascal;	state dead;
branches;
next	1.1;
commitid	PXOKb6wtLMa1GZnE;

1.1
date	2014.08.18.07.42.12;	author pascal;	state Exp;
branches;
next	;
commitid	XotHT24xfq7v9ugc;


desc
@@


1.2
log
@Update to 4.9.2.  Finally gets rid of gfortran patches.

Tested on everything except sparc, adastraps mirrored by and ok tobiasu@@
@
text
@$OpenBSD: patch-libgfortran_runtime_memory_c,v 1.1 2014/08/18 07:42:12 pascal Exp $
--- libgfortran/runtime/memory.c.orig	Thu Jan  2 23:25:45 2014
+++ libgfortran/runtime/memory.c	Thu Aug 14 16:53:59 2014
@@@@ -25,8 +25,13 @@@@ see the files COPYING3 and COPYING.RUNTIME respectivel
 
 #include "libgfortran.h"
 #include <stdlib.h>
+#include <errno.h>
 
+#ifndef SIZE_MAX
+#define SIZE_MAX ((size_t)-1)
+#endif
 
+
 void *
 xmalloc (size_t n)
 {
@@@@ -44,12 +49,34 @@@@ xmalloc (size_t n)
 }
 
 
+void *
+xmallocarray (size_t nmemb, size_t size)
+{
+  void *p;
+
+  if (!nmemb || !size)
+    size = nmemb = 1;
+  else if (nmemb > SIZE_MAX / size)
+    {
+      errno = ENOMEM;
+      os_error ("Integer overflow in xmallocarray");
+    }
+
+  p = malloc (nmemb * size);
+
+  if (!p)
+    os_error ("Memory allocation failed in xmallocarray");
+
+  return p;
+}
+
+
 /* calloc wrapper that aborts on error.  */
 
 void *
 xcalloc (size_t nmemb, size_t size)
 {
-  if (nmemb * size == 0)
+  if (!nmemb || !size)
     nmemb = size = 1;
 
   void *p = calloc (nmemb, size);
@


1.1
log
@Update to GCC 4.9.1 + fix for CVE-2014-5044.  As usual, sparc64 adastrap
generated by tobiasu@@, thanks!
@
text
@d1 1
a1 1
$OpenBSD$
@

