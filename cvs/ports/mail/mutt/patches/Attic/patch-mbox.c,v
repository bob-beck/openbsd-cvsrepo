head	1.2;
access;
symbols
	OPENBSD_2_6:1.1.0.2
	OPENBSD_2_6_BASE:1.1;
locks; strict;
comment	@ * @;


1.2
date	2000.01.21.16.39.39;	author espie;	state dead;
branches;
next	1.1;

1.1
date	99.09.10.10.05.56;	author turan;	state Exp;
branches;
next	;


desc
@@


1.2
log
@Update to mutt 1.0.1.

with help from David Terrell (and tests/advice from various people).
@
text
@--- mbox.c.orig	Wed Aug  4 15:52:22 1999
+++ mbox.c	Fri Sep 10 06:29:20 1999
@@@@ -275,7 +275,7 @@@@
 
       if (!ctx->quiet && ReadInc && ((count % ReadInc == 0) || count == 1))
 	mutt_message (_("Reading %s... %d (%d%%)"), ctx->path, count,
-		      ftell (ctx->fp) / (ctx->size / 100 + 1));
+		      (int)(ftell (ctx->fp) / (ctx->size / 100 + 1)));
 
       if (ctx->msgcount == ctx->hdrmax)
 	mx_alloc_memory (ctx);
@@@@ -765,7 +765,7 @@@@
       j++;
       if (!ctx->quiet && WriteInc && ((i % WriteInc) == 0 || j == 1))
 	mutt_message (_("Writing messages... %d (%d%%)"), i,
-		      ftell (ctx->fp) / (ctx->size / 100 + 1));
+		      (int)(ftell (ctx->fp) / (ctx->size / 100 + 1)));
 
       if (ctx->magic == M_MMDF)
       {
@


1.1
log
@long -> int
@
text
@@

