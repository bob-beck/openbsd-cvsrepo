head	1.4;
access;
symbols
	OPENBSD_3_8:1.3.0.2
	OPENBSD_3_8_BASE:1.3
	OPENBSD_3_7:1.2.0.14
	OPENBSD_3_7_BASE:1.2
	OPENBSD_3_6:1.2.0.12
	OPENBSD_3_6_BASE:1.2
	OPENBSD_3_5:1.2.0.10
	OPENBSD_3_5_BASE:1.2
	OPENBSD_3_4:1.2.0.8
	OPENBSD_3_4_BASE:1.2
	OPENBSD_3_3:1.2.0.6
	OPENBSD_3_3_BASE:1.2
	OPENBSD_3_2:1.2.0.4
	OPENBSD_3_2_BASE:1.2
	OPENBSD_3_1:1.2.0.2
	OPENBSD_3_1_BASE:1.2
	OPENBSD_3_0:1.1.0.2
	OPENBSD_3_0_BASE:1.1
	OPENBSD_2_9_TRACKING_SWITCH:1.1;
locks; strict;
comment	@# @;


1.4
date	2005.11.25.09.08.01;	author sturm;	state dead;
branches;
next	1.3;

1.3
date	2005.05.04.17.08.24;	author sturm;	state Exp;
branches;
next	1.2;

1.2
date	2002.01.05.21.29.25;	author naddy;	state Exp;
branches;
next	1.1;

1.1
date	2001.05.12.16.41.09;	author naddy;	state Exp;
branches;
next	;


desc
@@


1.4
log
@update to amanda 2.4.5p1, from maintainer
USE_LIBTOOL and add annotations for non-regular ownership/mode
@
text
@$OpenBSD: patch-config_ltmain_sh,v 1.3 2005/05/04 17:08:24 sturm Exp $
--- config/ltmain.sh.orig	Sun May  1 15:59:45 2005
+++ config/ltmain.sh	Sun May  1 16:00:00 2005
@@@@ -5824,40 +5824,6 @@@@ relink_command=\"$relink_command\""
     # Exit here if they wanted silent mode.
     test "$show" = : && exit $EXIT_SUCCESS
 
-    $echo "----------------------------------------------------------------------"
-    $echo "Libraries have been installed in:"
-    for libdir in $libdirs; do
-      $echo "   $libdir"
-    done
-    $echo
-    $echo "If you ever happen to want to link against installed libraries"
-    $echo "in a given directory, LIBDIR, you must either use libtool, and"
-    $echo "specify the full pathname of the library, or use the \`-LLIBDIR'"
-    $echo "flag during linking and do at least one of the following:"
-    if test -n "$shlibpath_var"; then
-      $echo "   - add LIBDIR to the \`$shlibpath_var' environment variable"
-      $echo "     during execution"
-    fi
-    if test -n "$runpath_var"; then
-      $echo "   - add LIBDIR to the \`$runpath_var' environment variable"
-      $echo "     during linking"
-    fi
-    if test -n "$hardcode_libdir_flag_spec"; then
-      libdir=LIBDIR
-      eval flag=\"$hardcode_libdir_flag_spec\"
-
-      $echo "   - use the \`$flag' linker flag"
-    fi
-    if test -n "$admincmds"; then
-      $echo "   - have your system administrator run these commands:$admincmds"
-    fi
-    if test -f /etc/ld.so.conf; then
-      $echo "   - have your system administrator add LIBDIR to \`/etc/ld.so.conf'"
-    fi
-    $echo
-    $echo "See any operating system documentation about shared libraries for"
-    $echo "more information, such as the ld(1) and ld.so(8) manual pages."
-    $echo "----------------------------------------------------------------------"
     exit $EXIT_SUCCESS
     ;;
 
@


1.3
log
@update to amanda 2.4.4p4

from new maintainer Olivier Cherrier <oc at symacx.com>
(old maintainer's email address is bouncing)
@
text
@d1 1
a1 1
$OpenBSD: patch-config_ltmain_sh,v 1.2 2002/01/05 21:29:25 naddy Exp $
@


1.2
log
@sync libtool patches
@
text
@d1 4
a4 50
$OpenBSD: patch-config_ltmain_sh,v 1.1 2001/05/12 16:41:09 naddy Exp $
--- config/ltmain.sh.orig	Tue Jun 13 00:58:01 2000
+++ config/ltmain.sh	Sat Jan  5 22:23:42 2002
@@@@ -1073,6 +1073,17 @@@@ compiler."
 	    # These systems don't actually have c library (as such)
 	    continue
 	    ;;
+	  *-*-openbsd*)
+	    # Do not include libc due to us having libc/libc_r.
+	    continue
+	    ;;
+	  esac
+	elif test "$arg" = "-lc_r"; then
+	  case "$host" in
+	  *-*-openbsd*)
+	    # Do not include libc_r directly, use -pthread flag.
+	    continue
+	    ;;
 	  esac
 	elif test "$arg" = "-lm"; then
 	  case "$host" in
@@@@ -1085,6 +1096,17 @@@@ compiler."
 	deplibs="$deplibs $arg"
 	;;
 
+      -pthread)
+	case $host in
+	*-*-openbsd*)
+	  deplibs="$deplibs $arg"
+	  ;;
+	*)
+	  continue
+	  ;;
+	esac
+	;;
+
       -module)
 	module=yes
 	continue
@@@@ -1789,6 +1811,9 @@@@ compiler."
 	*-*-cygwin* | *-*-mingw* | *-*-os2* | *-*-beos*)
 	  # these systems don't actually have a c library (as such)!
 	  ;;
+	*-*-openbsd*)
+	  # Do not include libc due to us having libc/libc_r.
+	  ;;
 	*)
 	  # Add libc to deplibs on all other systems.
 	  deplibs="$deplibs -lc"
@@@@ -3522,40 +3547,6 @@@@ libdir='$install_libdir'\
d6 1
a6 1
     test "$show" = : && exit 0
d8 2
a9 2
-    echo "----------------------------------------------------------------------"
-    echo "Libraries have been installed in:"
d11 1
a11 1
-      echo "   $libdir"
d13 5
a17 5
-    echo
-    echo "If you ever happen to want to link against installed libraries"
-    echo "in a given directory, LIBDIR, you must either use libtool, and"
-    echo "specify the full pathname of the library, or use \`-LLIBDIR'"
-    echo "flag during linking and do at least one of the following:"
d19 2
a20 2
-      echo "   - add LIBDIR to the \`$shlibpath_var' environment variable"
-      echo "     during execution"
d23 2
a24 2
-      echo "   - add LIBDIR to the \`$runpath_var' environment variable"
-      echo "     during linking"
d30 1
a30 1
-      echo "   - use the \`$flag' linker flag"
d33 1
a33 1
-      echo "   - have your system administrator run these commands:$admincmds"
d36 1
a36 1
-      echo "   - have your system administrator add LIBDIR to \`/etc/ld.so.conf'"
d38 5
a42 5
-    echo
-    echo "See any operating system documentation about shared libraries for"
-    echo "more information, such as the ld(1) and ld.so(8) manual pages."
-    echo "----------------------------------------------------------------------"
     exit 0
@


1.1
log
@Update amanda to 2.4.2p2.  Overhaul port completely.
Submitted by Tom Schutter <t.schutter@@att.net>.
@
text
@d1 1
a1 1
$OpenBSD$
d3 2
a4 2
+++ config/ltmain.sh	Mon Apr 30 20:17:06 2001
@@@@ -1073,7 +1073,18 @@@@ compiler."
d8 11
a18 4
+          *-*-openbsd*)
+            # Do not include libc due to us having libc/libc_r.
+            continue
+            ;;
a19 7
+        elif test "$arg" = "-lc_r"; then
+          case "$host" in
+          *-*-openbsd*)
+            # Do not include libc_r directly, use -pthread flag.
+            continue
+            ;;
+          esac
d22 1
a22 2
 	  *-*-cygwin* | *-*-beos*)
@@@@ -1085,6 +1096,10 @@@@ compiler."
d26 10
a35 3
+      -?thread)
+        deplibs="$deplibs $arg"
+        ;;
d40 1
a40 1
@@@@ -1789,6 +1804,9 @@@@ compiler."
d45 1
a45 1
+	  # do not include libc due to us having libc/libc_r.
d50 1
a50 1
@@@@ -3522,40 +3540,6 @@@@ libdir='$install_libdir'\
@

