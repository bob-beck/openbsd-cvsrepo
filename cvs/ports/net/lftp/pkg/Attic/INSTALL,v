head	1.3;
access;
symbols
	OPENBSD_3_6:1.2.0.12
	OPENBSD_3_6_BASE:1.2
	OPENBSD_3_5:1.2.0.10
	OPENBSD_3_5_BASE:1.2
	OPENBSD_3_4:1.2.0.8
	OPENBSD_3_4_BASE:1.2
	OPENBSD_3_3:1.2.0.6
	OPENBSD_3_3_BASE:1.2
	OPENBSD_3_2:1.2.0.4
	OPENBSD_3_2_BASE:1.2
	OPENBSD_3_1:1.2.0.2
	OPENBSD_3_1_BASE:1.2
	OPENBSD_3_0:1.1.0.2
	OPENBSD_3_0_BASE:1.1;
locks; strict;
comment	@# @;


1.3
date	2004.09.22.22.39.18;	author espie;	state dead;
branches;
next	1.2;

1.2
date	2001.10.26.09.03.13;	author kevlo;	state Exp;
branches;
next	1.1;

1.1
date	2001.10.07.19.01.56;	author heko;	state Exp;
branches;
next	;


desc
@@


1.3
log
@kill INSTALL script
@
text
@#!/bin/sh
# $OpenBSD: INSTALL,v 1.2 2001/10/26 09:03:13 kevlo Exp $
#
# Pre/post-installation setup of lftp

# exit on errors, use a sane path and install prefix
#
set -e
PATH=/bin:/usr/bin:/sbin:/usr/sbin
PREFIX=${PKG_PREFIX:-/usr/local}
CONFIG_FILE=${SYSCONFDIR}/lftp.conf
SAMPLE_CONFIG_FILE=$PREFIX/share/examples/lftp/lftp.conf-sample

do_notice()
{
    echo
    echo "+---------------"
    echo "| The existing $1 configuration file, $CONFIG_FILE,"
    echo "| has NOT been changed. You may want to compare it to the"
    echo "| current sample file, $SAMPLE_CONFIG_FILE,"
    echo "| and update your configuration as needed."
    echo "+---------------"
    echo
}

do_install()
{
    install -o root -g wheel -m 644 $SAMPLE_CONFIG_FILE $CONFIG_FILE
    echo
    echo "+---------------"
    echo "| The $1 configuration file, $CONFIG_FILE," 
    echo "| has been installed.  Please view this file and change"
    echo "| the configuration to meet your needs."
    echo "+---------------"
    echo
}

# verify proper execution
#
if [ $# -ne 2 ]; then
    echo "usage: $0 distname { PRE-INSTALL | POST-INSTALL }" >&2
    exit 1
fi

# Verify/process the command
#
case $2 in
    PRE-INSTALL)
        : nothing to pre-install for this port
        ;;
    POST-INSTALL)
	if [ -f $CONFIG_FILE ]; then
	    do_notice $1
	else
	    do_install $1
	fi
        ;;
    *)
        echo "usage: $0 distname { PRE-INSTALL | POST-INSTALL }" >&2
        exit 1
        ;;
esac

exit 0

@


1.2
log
@- upgrade to version 2.4.5
- add two ftp sites
- forgot to install lftp.conf; Pointed out by
  Hannu Liljemark <hannu.liljemark@@kolumbus.fi>
@
text
@d2 1
a2 1
# $OpenBSD: INSTALL,v 1.1 2001/10/07 19:01:56 heko Exp $
@


1.1
log
@o Use ${PREFIX}/share/examples/lftp for examples,
  not ${PREFIX}/lib/lftp
o Use {INSTALL,DEINSTALL} for notification messages for config files
@
text
@d2 1
a2 1
# $OpenBSD$
d11 2
a12 2
CONFIG_DIR=${SYSCONFDIR}
SAMPLE_CONFIG_DIR=$PREFIX/share/examples/lftp
d18 1
a18 1
    echo "| The existing configuration file in $CONFIG_DIR/lftp.conf"
d20 1
a20 1
    echo "| current sample file in $SAMPLE_CONFIG_DIR/lftp.conf,"
d28 1
a28 2
    install -d -o root -g wheel -m 755 $CONFIG_DIR
    install -o root -g wheel -m 644 $SAMPLE_CONFIG_DIR/lftp.conf $CONFIG_DIR/lftp.conf
d31 3
a33 3
    echo "| The $1 configuration file has been installed into"
    echo "| $CONFIG_DIR/lftp.conf. Please view this file and change the configuration"
    echo "| to meet your needs."
d52 5
a56 7
        if [ ! -d $CONFIG_DIR ]; then
            do_install $1
        elif [ ! -f $CONFIG_DIR/lftp.conf ]; then
            do_install $1
        else
            do_notice $1
        fi
@

