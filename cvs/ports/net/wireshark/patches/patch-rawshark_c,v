head	1.1;
access;
symbols;
locks; strict;
comment	@# @;


1.1
date	2017.09.01.09.39.29;	author sthen;	state Exp;
branches;
next	;
commitid	yOtUOZBjbEC8glK4;


desc
@@


1.1
log
@unbreak; forgotten "cvs add"
@
text
@$OpenBSD$

Index: rawshark.c
--- rawshark.c.orig
+++ rawshark.c
@@@@ -602,7 +602,11 @@@@ main(int argc, char *argv[])
                 limit.rlim_cur = get_positive_int(optarg, "memory limit");
                 limit.rlim_max = get_positive_int(optarg, "memory limit");
 
+#ifdef RLIMIT_AS
                 if(setrlimit(RLIMIT_AS, &limit) != 0) {
+#else
+                if(setrlimit(RLIMIT_DATA, &limit) != 0) {
+#endif
                     cmdarg_err("setrlimit() returned error");
                     ret = INVALID_OPTION;
                     goto clean_exit;
@
