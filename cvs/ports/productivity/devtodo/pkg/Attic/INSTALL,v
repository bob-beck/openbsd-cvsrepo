head	1.4;
access;
symbols
	OPENBSD_3_6:1.3.0.8
	OPENBSD_3_6_BASE:1.3
	OPENBSD_3_5:1.3.0.6
	OPENBSD_3_5_BASE:1.3
	OPENBSD_3_4:1.3.0.4
	OPENBSD_3_4_BASE:1.3
	OPENBSD_3_3:1.3.0.2
	OPENBSD_3_3_BASE:1.3
	OPENBSD_3_2:1.1.1.1.0.2
	OPENBSD_3_2_BASE:1.1.1.1
	lebel_2002-Aug-27:1.1.1.1
	lebel:1.1.1;
locks; strict;
comment	@# @;


1.4
date	2004.09.29.13.19.19;	author xsa;	state dead;
branches;
next	1.3;

1.3
date	2002.12.02.13.30.09;	author margarida;	state Exp;
branches;
next	1.2;

1.2
date	2002.12.01.19.18.22;	author margarida;	state Exp;
branches;
next	1.1;

1.1
date	2002.08.28.01.41.11;	author lebel;	state Exp;
branches
	1.1.1.1;
next	;

1.1.1.1
date	2002.08.28.01.41.11;	author lebel;	state Exp;
branches;
next	;


desc
@@


1.4
log
@bye bye INSTALL;
@
text
@#!/bin/sh
# $OpenBSD: INSTALL,v 1.3 2002/12/02 13:30:09 margarida Exp $
#
# Pre/post-installation setup of devtodo

# exit on errors, use a sane path and install prefix
#
set -e
PATH=/bin:/usr/bin:/sbin:/usr/sbin
PREFIX=${PKG_PREFIX:-/usr/local}
CONFIG_FILE=${SYSCONFDIR}/todorc
SAMPLE_CONFIG_FILE=$PREFIX/share/examples/devtodo/todorc.example

do_notice()
{
	echo
	echo "+------------"
	echo "| The existing $1 configuration file in $CONFIG_FILE,"
	echo "| has NOT been changed." 
	echo "|	You may want to compare it to the current sample"
	echo "| file in $SAMPLE_CONFIG_FILE,"
	echo "| and update your configuration as needed."
	echo "+------------"
}

do_install()
{
	install -o root -g wheel $SAMPLE_CONFIG_FILE $CONFIG_FILE
	echo
	echo "+------------"
	echo "| The $1 configuration file has been installed"
	echo "| into $CONFIG_FILE. Please view this file and"
	echo "| change the configuration to meet your needs."
	echo "| You can also place it in ~/.todorc."
	echo "+------------"
}

# verify proper execution
#
if [ $# -ne 2 ]; then
	echo "usage: $0 distname { PRE-INSTALL | POST-INSTALL }" >&2
    exit 1
fi

# Verify/process the command
#
case $2 in
	PRE-INSTALL)
		: nothing to pre-install for this port
		;;
	POST-INSTALL)
		if [ ! -f $CONFIG_FILE ]; then
			do_install $1
		else
			do_notice $1
		fi
		;;
	*)
		echo "usage: $0 distname { PRE-INSTALL | POST-INSTALL }" >&2
		exit 1
	;;
esac

exit 0
@


1.3
log
@Missing ending quotes.
Pointed by Jim Geovedi. Thanks!
@
text
@d2 1
a2 1
# $OpenBSD: INSTALL,v 1.2 2002/12/01 19:18:22 margarida Exp $
@


1.2
log
@Remove hardcoded installation.
Should look for a file, not a directory.
Cosmetic changes.

pvalchev@@ ok
@
text
@d2 1
a2 1
# $OpenBSD: INSTALL,v 1.1.1.1 2002/08/28 01:41:11 lebel Exp $
d19 1
a19 1
	echo "| has NOT been changed. 
@


1.1
log
@Initial revision
@
text
@d2 1
a2 1
# $OpenBSD$
d11 2
a12 2
CONFIG_DIR=${SYSCONFDIR}
SAMPLE_CONFIG_DIR=${PREFIX}/share/examples/devtodo
d18 4
a21 3
	echo "| The existing $1 configuration file in ${CONFIG_DIR},"
	echo "| has NOT been changed. You may want to compare them"
	echo "| to the current sample file in ${SAMPLE_CONFIG_DIR},"
d28 1
a28 2
	install -d -o root -g wheel -m 755 ${CONFIG_DIR}
	install -o root -g wheel -m 644 ${SAMPLE_CONFIG_DIR}/todorc.example ${CONFIG_DIR}/todorc
d31 3
a33 3
	echo "| The $1 configuration file (todorc) has been installed"
	echo "| into ${CONFIG_DIR}. Please view this file and change the"
	echo "| configuration to meet your needs."
d52 1
a52 3
		if [ ! -d $CONFIG_DIR ]; then
			do_install $1
		elif [ ! -f $CONFIG_DIR/todorc ]; then
@


1.1.1.1
log
@initial import of productivity/devtodo 0.1.14:
--
Todo is a program to display and manage a hierarchical list of outstanding 
work, or just reminders.

The program itself is assisted by a few shell scripts that override default
builtins. Specifically, cd, pushd and popd are overridden so that when using
one of these commands to enter a directory, the todo will display any
outstanding items in that directory.

WWW: http://devtodo.sourceforge.net/

Submitted by Margarida Sequeira <niness@@devilness.org>

@
text
@@
