head	1.2;
access;
symbols
	OPENBSD_2_7:1.1.0.2
	OPENBSD_2_7_BASE:1.1;
locks; strict;
comment	@# @;


1.2
date	2000.09.07.05.44.32;	author brad;	state dead;
branches;
next	1.1;

1.1
date	99.12.13.03.58.19;	author brad;	state Exp;
branches;
next	;


desc
@@


1.2
log
@- remove a bunch of unused variables
- remove unnecessary patch (patch-aa)
- use better filename for patch
@
text
@--- makefile.orig	Sun Dec 12 22:09:37 1999
+++ makefile	Sun Dec 12 22:14:36 1999
@@@@ -50,13 +50,13 @@@@
 # -DUSE_SELECT to use select() system call
 # -DUSE_NBIO   to use non-blocking read()
 
-CFLAGS= -O -DUNIX -DPORTABLE $(BYTEORDER)
+#CFLAGS= -O -DUNIX -DPORTABLE $(BYTEORDER)
 
 # must set byte order for targets "sysv" and "bsd"
 # BYTEORDER= -DHIGHFIRST
 
-CC      = cc
-LD      = cc            # Link command
+#CC      = cc
+LD      = $(CC)		# Link command
 LDFLAGS = 
 CPP     = $(CC) -E
 DBG     = -O
@@@@ -97,8 +97,8 @@@@
 all: $(PROJ)
 
 # For a pure MPILIB version, uncomment the following lines
-RSALIBS =
-RSAOBJS = rsaglue1.o
+#RSALIBS =
+#RSAOBJS = rsaglue1.o
 
 # For an RSAREF version, uncomment the following lines
 #RSADIR = ../rsaref
@@@@ -146,7 +146,7 @@@@
 	randpool.o noise.o
 
 OBJS =  $(OBJ1) $(ZIPOBJS) $(RSAOBJS) $(OBJS_EXT)
-CFLAGS = $(CFLAGS) -I$(RSAINCDIR)
+#CFLAGS = $(CFLAGS) -I$(RSAINCDIR)
 
 $(PROJ):	$(OBJS)
 		$(LD) -o $(PROJ) $(OBJS) $(LDFLAGS) $(LIBS_EXT) $(RSALIBS)
@


1.1
log
@- remove now unnecessary PATCH_LIST tag
- BUILD_DEPENDS -> LIB_DEPENDS, rsaref makes a shared library
- add patch so that pgp respects CC
@
text
@@

