head	1.3;
access;
symbols
	OPENBSD_2_7:1.2.0.2
	OPENBSD_2_7_BASE:1.2
	OPENBSD_2_6:1.1.0.4
	OPENBSD_2_6_BASE:1.1
	OPENBSD_2_5:1.1.0.2
	OPENBSD_2_5_BASE:1.1;
locks; strict;
comment	@# @;


1.3
date	2000.08.27.20.45.16;	author turan;	state dead;
branches;
next	1.2;

1.2
date	2000.03.25.05.52.39;	author brad;	state Exp;
branches;
next	1.1;

1.1
date	99.04.08.05.36.12;	author marc;	state Exp;
branches;
next	;


desc
@@


1.3
log
@remove ssh port.  discussed with theo.
@
text
@#!/bin/sh
# $OpenBSD: INSTALL,v 1.2 2000/03/25 05:52:39 brad Exp $
#
# Pre/post-installation setup of ssh

# exit on errors, use a sane path and install prefix
#
set -e
PATH=/bin:/usr/bin:/sbin:/usr/sbin
PREFIX=${PKG_PREFIX:-/usr/local}
PIDFILE=/var/run/sshd.pid

# Function: tell the user what s/he needs to do to use the port just installed
#
do_notice()
{
    echo
    echo "+---------------"
    echo "| To start $1 run the following command as root"
    echo "|"
    echo "|        ${PREFIX}/sbin/sshd"
    echo "|"
    echo "| This is done automatically in the stock /etc/rc"
    echo "| whenever you restart your system"
    echo "+---------------"
    echo
}

# verify proper execution
#
if [ $# -ne 2 ]; then
    echo "usage: $0 distname { PRE-INSTALL | POST-INSTALL }" >&2
    exit 1
fi

# Verify/process the command
#
case $2 in
    PRE-INSTALL)
	: nothing to pre-install for this port
	;;
    POST-INSTALL)
	if [ ! -f $PIDFILE ]; then
	    do_notice $1
	fi
	;;
    *)
	echo "usage: $0 distname { PRE-INSTALL | POST-INSTALL }" >&2
	exit 1
	;;
esac

exit 0
@


1.2
log
@- rearrange
- fix comment
@
text
@d2 1
a2 1
# $OpenBSD: INSTALL,v 1.1 1999/04/08 05:36:12 marc Exp $
@


1.1
log
@o add INSTALL script that checks if sshd running.  If not spit
  out a message saying how to start the daemon
o call the INSTALL script from the Makefile
o verify the script is processed when installing the ssh package
THIS PORT IS NOW FROZEN FOR 2.5
@
text
@d2 1
a2 1
# $OpenBSD: INSTALL,v 1.1 1999/04/08 04:37:27 marc Exp $
d6 7
d23 1
a23 1
    echo "| This is done automatically in the stock /etc/rc.local"
a27 7

# exit on errors, use a sane path and install prefix
#
set -e
PATH=/bin:/usr/bin:/sbin:/usr/sbin
PREFIX=${PKG_PREFIX:-/usr/local}
PIDFILE=/var/run/sshd.pid
@

