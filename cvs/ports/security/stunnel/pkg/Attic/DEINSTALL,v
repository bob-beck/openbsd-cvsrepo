head	1.3;
access;
symbols
	OPENBSD_3_6:1.2.0.2
	OPENBSD_3_6_BASE:1.2
	OPENBSD_3_5:1.1.0.2
	OPENBSD_3_5_BASE:1.1;
locks; strict;
comment	@# @;


1.3
date	2004.11.28.16.32.28;	author alek;	state dead;
branches;
next	1.2;

1.2
date	2004.04.15.08.58.33;	author xsa;	state Exp;
branches;
next	1.1;

1.1
date	2004.02.16.12.33.18;	author jakob;	state Exp;
branches;
next	;


desc
@@


1.3
log
@- Kill DEINSTALL
- Replace INSTALL with @@newgroup, @@newuser and @@sample
- Add WANTLIB marker
- Bump PKGNAME

ok jakob@@
@
text
@#!/bin/sh
# $OpenBSD: DEINSTALL
#
# Stunnel de-installation

PATH=/bin:/usr/bin:/sbin:/usr/sbin
CONFIG_DIR=${SYSCONFDIR}/stunnel
CHROOT_DIR=/var/stunnel
STUNNELUSER=_stunnel
STUNNELGROUP=_stunnel

if [ ${PKG_DELETE_EXTRA} != Yes -a -d $CONFIG_DIR ]; then
	echo
	echo "+---------------"
	echo "| To completely deinstall the $1 package you need to perform"
	echo "| these steps as root (make sure stunnel is not running!):"
	echo "|"
	echo "|          userdel $STUNNELUSER"
	echo "|          groupdel $STUNNELGROUP"
	echo "|          rm -rf $CONFIG_DIR"
	echo "|          rm -rf $CHROOT_DIR"
	echo "|          rm /var/run/stunnel.pid"
	echo "|"
	echo "| Do not do this if you plan on re-installing $1"
	echo "| at some future time."
	echo "+---------------"
	echo
fi

exit 0
@


1.2
log
@
catch @@extra escapers ...
@
text
@@


1.1
log
@update to v4.05
add chroot/privdrop from Michael Schubert
@
text
@d12 17
a28 13
echo
echo " To completely deinstall the $1 package you need to perform"
echo " these steps as root (make sure stunnel is not running!):"
echo ""
echo "           userdel $STUNNELUSER"
echo "           groupdel $STUNNELGROUP"
echo "           rm -rf $CONFIG_DIR"
echo "           rm -rf $CHROOT_DIR"
echo "           rm /var/run/stunnel.pid"
echo ""
echo " Do not do this if you plan on re-installing $1"
echo " at some future time."
echo
@

