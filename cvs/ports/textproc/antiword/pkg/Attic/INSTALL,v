head	1.2;
access;
symbols;
locks; strict;
comment	@# @;


1.2
date	2001.02.18.04.40.52;	author brad;	state dead;
branches;
next	1.1;

1.1
date	2001.02.18.00.25.32;	author obecian;	state Exp;
branches;
next	;


desc
@@


1.2
log
@I don't see the point of an INSTALL script in this situation, this
arrangement makes much more sense.
@
text
@#!/bin/sh
# $OpenBSD: INSTALL,v 1.1 2001/02/18 00:25:32 obecian Exp $
#
# Pre/post-installation setup of 

PATH=/bin:/usr/bin:/sbin:/usr/sbin
PREFIX=${PKG_PREFIX:-/usr/local}
CONFIG_DIR=${PREFIX}/share/antiword/resources

# Function: tell the user what s/he needs to do to use the port just installed
#
do_notice()
{
	echo ""
	echo "+---------------"
	echo "| Antiword resources are in $PREFIX/share/antiword/resources"
  echo "|"
	echo "| Copy the files from this directory into \$HOME/.antiword, for"
	echo "| each user wishing to use antiword."
	echo "|"
	echo "| This allows each user to customize antiword how s/he wants."
	echo "+---------------"
	echo ""
}

# Verify proper execution
#
if [ $# -ne 2 ]; then
	echo "usage: $0 distname { PRE-INSTALL | POST-INSTALL }" >&2
	exit 1
fi

# Verify/process the command
#
case $2 in
	PRE-INSTALL)
		: nothing to pre-install for this port
		;;
	POST-INSTALL)
		if [ -d ${CONFIG_DIR} ]; then
			do_notice "$1"
		else
			: we should never reach here
		fi
		;;
 	*)
		echo "usage: $0 distname { PRE-INSTALL | POST-INSTALL }" >&2
		exit 1
		;;
esac

exit 0
@


1.1
log
@add antiword resource properties and do_notice() to INSTALL regarding these
resources.
@
text
@d2 1
a2 1
# $OpenBSD$
@

