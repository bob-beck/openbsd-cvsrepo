head	1.2;
access;
symbols
	OPENBSD_5_4:1.1.1.1.0.4
	OPENBSD_5_4_BASE:1.1.1.1
	OPENBSD_5_3:1.1.1.1.0.2
	OPENBSD_5_3_BASE:1.1.1.1
	rpe_20121026:1.1.1.1
	rpe:1.1.1;
locks; strict;
comment	@# @;


1.2
date	2014.01.23.23.58.34;	author sthen;	state dead;
branches;
next	1.1;

1.1
date	2012.10.26.16.32.10;	author rpe;	state Exp;
branches
	1.1.1.1;
next	;

1.1.1.1
date	2012.10.26.16.32.10;	author rpe;	state Exp;
branches;
next	;


desc
@@


1.2
log
@update to haserl 0.9.32, no need for a patch to autoconf input to select
the lua version any more, upstream now accepts the pkg-config name as
a configure argument (because every OS uses a different name for this..)
@
text
@$OpenBSD: patch-configure_ac,v 1.1 2012/10/26 16:32:10 rpe Exp $
--- configure.ac.orig	Tue May 10 17:31:21 2011
+++ configure.ac	Tue Aug 21 17:56:43 2012
@@@@ -64,14 +64,21 @@@@ AC_ARG_WITH(lua, 
 	# ubuntu has lua5.1 rather than just lua
 	if pkg-config --exists lua5.1; then
 		LUALIB=lua5.1
+		LUAPC=lua5.1
+	# openbsd has liblua5.1.so but lua51.pc
+	else if pkg-config --exists lua51; then
+		LUALIB=lua5.1
+		LUAPC=lua51
 	else
 		LUALIB=lua
+		LUAPC=lua
 	fi
+	fi
 	if test -z "$LUA_HDR_DIR"; then
-		CFLAGS="$CFLAGS `pkg-config $LUALIB --cflags`"
+		CFLAGS="$CFLAGS `pkg-config $LUAPC --cflags`"
 	fi
 	LIBS="$LIBS -lm"
-	LDFLAGS="$LDFLAGS -Wl,-E -L$withval"
+	LDFLAGS="$LDFLAGS -Wl,-E `pkg-config $LUAPC --libs`"
 	AC_DEFINE(USE_LUA, , [Enable Lua])
 
 	AC_CHECK_LIB($LUALIB, luaL_newstate, , [
@


1.1
log
@Initial revision
@
text
@d1 1
a1 1
$OpenBSD$
@


1.1.1.1
log
@add from openbsd-wip git repo

ok sthen@@ (Maintainer)
@
text
@@
