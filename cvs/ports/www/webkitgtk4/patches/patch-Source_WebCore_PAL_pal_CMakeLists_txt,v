head	1.1;
access;
symbols;
locks; strict;
comment	@# @;


1.1
date	2017.05.13.15.47.06;	author espie;	state Exp;
branches;
next	;
commitid	tnIEwJOHe5nqPBUE;


desc
@@


1.1
log
@clang does not look into /usr/local by default, so let it build with clang
@
text
@$OpenBSD$

Index: Source/WebCore/PAL/pal/CMakeLists.txt
--- Source/WebCore/PAL/pal/CMakeLists.txt.orig
+++ Source/WebCore/PAL/pal/CMakeLists.txt
@@@@ -5,6 +5,7 @@@@ set(PAL_INCLUDE_DIRECTORIES
     "${PAL_DIR}"
     "${PAL_DIR}/pal"
     "${PAL_DIR}/pal/crypto"
+    ${LIBGCRYPT_INCLUDE_DIRS}
 )
 
 set(PAL_LIBRARIES
@
