head	1.2;
access;
symbols;
locks; strict;
comment	@# @;


1.2
date	2017.05.03.15.55.54;	author espie;	state dead;
branches;
next	1.1;
commitid	FD4F9hHJGGGsH4c1;

1.1
date	2017.05.03.13.50.47;	author espie;	state Exp;
branches;
next	;
commitid	WANvgj3VFnIzkouC;


desc
@@


1.2
log
@erase the port itself, already unlinked and quirked
@
text
@$OpenBSD: patch-write_c,v 1.1 2017/05/03 13:50:47 espie Exp $

Index: write.c
--- write.c.orig
+++ write.c
@@@@ -124,6 +124,7 @@@@ writeint(intValue, outfile)
 }
 
 
+void
 writeSimple(image, outfile)
   imageInfo *image;
   FILE *outfile;
@@@@ -408,7 +409,7 @@@@ writePostscript(image, outfile, encode, encapsulate, p
   int lshift, lmask;
   int depth, bpl, spb;
   int reverse;
-  long p;
+  time_t p;
   /* rle variables */
   int rlecount;
   byte rlesample;
@@@@ -741,7 +742,7 @@@@ writeColorPS(image, outfile, encode, encapsulate, prev
   double xdpi, ydpi, xscale, yscale, f;
   double left, top;
   int depth, bpl, spb;
-  long p;
+  time_t p;
   /* rle variables */
   int rlecount;
   dw  rletotal;
@@@@ -1165,6 +1166,7 @@@@ writeColorPS(image, outfile, encode, encapsulate, prev
  * Write an image in 'puzzle' format, suitable for loading with
  * "puzzle -picture".
  */
+void
 writePuzzle(image, outfile)
   imageInfo *image;
   FILE *outfile;
@@@@ -1327,6 +1329,7 @@@@ writeXWD(image, outfile, xyformat)
  * use it.
  */
 
+void
 writeXYPixmap(image, outfile)
   imageInfo *image;
   FILE *outfile;
@@@@ -1489,7 +1492,7 @@@@ writePPM(image, outfile)
 {
   XImage *ximage = image->ximage;
   int nc, width, height, w, h, cidx;
-  long p;
+  time_t p;
   int count;
 
   if (verbose) {
@


1.1
log
@fix enough warnings/errors that this compiles with clang
@
text
@d1 1
a1 1
$OpenBSD$
@

