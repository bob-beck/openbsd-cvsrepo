head	1.4;
access;
symbols
	OPENBSD_2_1:1.2.0.4
	OPENBSD_2_1_BASE:1.2
	OPENBSD_2_0:1.2.0.2
	OPENBSD_2_0_BASE:1.2;
locks; strict;
comment	@# @;


1.4
date	97.07.27.23.29.13;	author downsj;	state dead;
branches;
next	1.3;

1.3
date	97.07.27.22.37.02;	author downsj;	state Exp;
branches;
next	1.2;

1.2
date	96.08.16.17.58.44;	author michaels;	state Exp;
branches;
next	1.1;

1.1
date	96.05.22.11.36.27;	author deraadt;	state Exp;
branches;
next	;


desc
@@


1.4
log
@move recover script ala 1.79.
@
text
@#!/bin/sh -
#
#	$OpenBSD: recover.script,v 1.3 1997/07/27 22:37:02 downsj Exp $
#	@@(#)recover.in	8.8 (Berkeley) 10/10/96
#
# Script to recover nvi edit sessions.
#
RECDIR="/var/tmp/vi.recover"
SENDMAIL="/usr/sbin/sendmail"

echo 'Recovering nvi editor sessions.'

# Check editor backup files.
vibackup=`echo $RECDIR/vi.*`
if [ "$vibackup" != "$RECDIR/vi.*" ]; then
	for i in $vibackup; do
		# Only test files that are readable.
		if test ! -r $i; then
			continue
		fi

		# Unmodified nvi editor backup files either have the
		# execute bit set or are zero length.  Delete them.
		if test -x $i -o ! -s $i; then
			rm $i
		fi
	done
fi

# It is possible to get incomplete recovery files, if the editor crashes
# at the right time.
virecovery=`echo $RECDIR/recover.*`
if [ "$virecovery" != "$RECDIR/recover.*" ]; then
	for i in $virecovery; do
		# Only test files that are readable.
		if test ! -r $i; then
			continue
		fi

		# Delete any recovery files that are zero length, corrupted,
		# or that have no corresponding backup file.  Else send mail
		# to the user.
		recfile=`awk '/^X-vi-recover-path:/{print $2}' < $i`
		if test -n "$recfile" -a -s "$recfile"; then
			$SENDMAIL -t < $i
		else
			rm $i
		fi
	done
fi
@


1.3
log
@sync
@
text
@d3 1
a3 1
#	$OpenBSD$
@


1.2
log
@nvi 1.73
@
text
@d1 4
a4 1
#	@@(#)recover.script	8.7 (Berkeley) 8/16/94
d8 3
a10 2
RECDIR=/var/tmp/vi.recover
SENDMAIL=/usr/sbin/sendmail
@


1.1
log
@new vi
@
text
@d6 1
a6 1
SENDMAIL=/usr/lib/sendmail
@
